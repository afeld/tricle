<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <table>
      <tr>
        <th rowspan="2"><!--Name--></th>
        <th>Last week</th>
        <th>Previous week</th>
        <th>Quarterly average</th>
      </tr>
      <tr>
        <th><%= last_week_dates_str %></th>
        <th><%= previous_week_dates_str %></th>
        <th><%= quarter_dates_str %></th>
      </tr>
      <% @metrics.each do |metric| %>
        <tr>
          <th rowspan="2"><%= metric.title %></th>
          <td><%= metric.last_week %></td>
          <td><%= percent_change(metric.last_week, metric.weeks_ago(2)) %></td>
          <td><%= percent_change(metric.last_week, metric.week_average_this_quarter) %></td>
        </tr>
        <tr>
          <td><%= metric.total %> (total)</td>
          <td><%= metric.weeks_ago(2) %></td>
          <td><%= metric.week_average_this_quarter.round %></td>
        </tr>
      <% end %>
    </table>
  </body>
</html>
