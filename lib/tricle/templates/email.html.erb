<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <% @metrics.each do |metric| %>
      <%= metric.for_range(Time.now, Time.now) %>
    <% end %>
  </body>
</html>
